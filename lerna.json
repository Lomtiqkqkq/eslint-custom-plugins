{
  "version": "independent",
  "packages": [
    "packages/*"
  ],
  "command": {
    "version": {
      "conventionalCommits": true
    },
    "publish": {
      "ignoreChanges": ["*.md"],
      "message": "chore(release): publish",
      "push": true
    }
  }
}
